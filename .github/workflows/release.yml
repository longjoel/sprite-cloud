name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  build-server:
    name: Build gv-server
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            arch: x86_64
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libgstreamer1.0-dev libgstreamer-plugins-base1.0-dev \
            gstreamer1.0-plugins-base gstreamer1.0-plugins-good \
            gstreamer1.0-plugins-ugly \
            libglib2.0-dev pkg-config

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2

      - name: Build gv-server
        run: cargo build --release -p gv-server --target ${{ matrix.target }}

      - name: Upload binary as release asset
        uses: actions/upload-artifact@v4
        with:
          name: ${{ matrix.arch }}
          path: target/${{ matrix.target }}/release/gv-server

  build-appimage:
    name: Build AppImage (${{ matrix.arch }})
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - target: x86_64-unknown-linux-gnu
            arch: x86_64
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies (Tauri + AppImage)
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            libwebkit2gtk-4.1-dev libgtk-3-dev \
            libayatana-appindicator3-dev librsvg2-dev \
            patchelf libudev-dev \
            libfuse2  # AppImage runtime

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Cache cargo
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: gv-desktop

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: latest

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install frontend dependencies
        run: pnpm install --frozen-lockfile

      - name: Build frontend (required by Tauri embed)
        run: pnpm --filter gv-web build

      - name: Install tauri-cli
        run: cargo install tauri-cli --version "^2" --locked

      - name: Build AppImage
        run: |
          cd gv-desktop
          cargo tauri build --bundles appimage
          ls -lh target/release/bundle/appimage/

      - name: Upload AppImage artifact
        uses: actions/upload-artifact@v4
        with:
          name: appimage-${{ matrix.arch }}
          path: gv-desktop/target/release/bundle/appimage/games-vault_*.AppImage

  create-release:
    name: Create GitHub Release
    needs: [build-server, build-appimage]
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Organize and checksum artifacts
        run: |
          set -euo pipefail
          mkdir -p release-assets

          # gv-server binaries
          if [ -d artifacts/x86_64 ]; then
            cp artifacts/x86_64/gv-server release-assets/gv-server-x86_64
            sha256sum release-assets/gv-server-x86_64 > release-assets/gv-server-x86_64.sha256
          fi

          # AppImage
          APPIMAGE=$(find artifacts/appimage-x86_64 -name 'games-vault_*.AppImage' 2>/dev/null | head -1)
          if [ -n "$APPIMAGE" ]; then
            VERSION="${GITHUB_REF_NAME#v}"
            cp "$APPIMAGE" "release-assets/games-vault-${VERSION}-x86_64.AppImage"
            sha256sum "release-assets/games-vault-${VERSION}-x86_64.AppImage" > "release-assets/games-vault-${VERSION}-x86_64.AppImage.sha256"
          fi

          ls -la release-assets/

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          name: ${{ github.ref_name }}
          body_path: CHANGELOG.md
          draft: ${{ github.event_name == 'push' && !startsWith(github.ref, 'refs/tags/v') }}
          prerelease: ${{ contains(github.ref_name, '-rc') || contains(github.ref_name, '-beta') || contains(github.ref_name, '-alpha') }}
          files: |
            release-assets/*
