[package]
name = "libretro-runner"
version = "0.1.0"
edition = "2024"
description = "Synchronous libretro core runner — loads cores, runs them frame-by-frame, captures video and audio"

[dependencies]
libloading = "0.8"
bytemuck = "1"
thiserror = "2"
tracing = "0.1"
