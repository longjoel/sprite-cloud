# Rust build artifacts (huge)
target/
.release/

# Next.js build cache (regenerated in Docker)
gv-web/.next/cache/
gv-web/.next/server/
gv-web/.next/types/
gv-web/.next/diagnostics/
gv-web/.next/*.json

# Dependencies (copied from builder stage)
node_modules/
gv-web/node_modules/
gv-player/node_modules/

# Git
.git/
.gitignore

# Environment secrets
.env
.env.local
*.env

# IDE
.idea/
.vscode/
*.swp

# Hermes
.hermes/

# Docs (not needed at build time)
docs/
