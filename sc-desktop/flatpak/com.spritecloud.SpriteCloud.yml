app-id: org.spritecloud.SpriteCloud
runtime: org.gnome.Platform
runtime-version: '48'
sdk: org.gnome.Sdk
sdk-extensions:
  - org.freedesktop.Sdk.Extension.rust-stable
command: sprite-cloud
finish-args:
  - --share=network
  - --share=ipc
  - --socket=wayland
  - --socket=x11
  - --socket=pulseaudio
  - --device=dri
  - --talk-name=org.freedesktop.secrets
  - --env=GDK_BACKEND=x11
  - --env=GSK_RENDERER=cairo
modules:
  - name: sprite-cloud
    buildsystem: simple
    build-commands:
      - |
        set -e
        . /usr/lib/sdk/rust-stable/enable.sh
        cargo build --release --manifest-path Cargo.toml --features app_debug
        install -Dm755 target/release/sc-desktop /app/bin/sprite-cloud
        install -Dm644 icons/32x32.png /app/share/icons/hicolor/32x32/apps/org.spritecloud.SpriteCloud.png
        install -Dm644 icons/128x128.png /app/share/icons/hicolor/128x128/apps/org.spritecloud.SpriteCloud.png
        install -Dm644 icons/128x128@2x.png /app/share/icons/hicolor/256x256/apps/org.spritecloud.SpriteCloud.png
        install -Dm644 flatpak/org.spritecloud.SpriteCloud.desktop /app/share/applications/org.spritecloud.SpriteCloud.desktop
        install -Dm644 flatpak/org.spritecloud.SpriteCloud.metainfo.xml /app/share/metainfo/org.spritecloud.SpriteCloud.metainfo.xml
    sources:
      - type: dir
        path: ..
    build-options:
      build-args:
        - --share=network
